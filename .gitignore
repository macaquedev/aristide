/target
/reference

# Sample sets are large and externally sourced — ignored. But Aristide
# sidecar files are small, hand-written config: those we do version.
/testsets/**
!/testsets/**/
!/testsets/**/*.aristide.toml
