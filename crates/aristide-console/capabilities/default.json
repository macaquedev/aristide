{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The console window: core APIs plus the native file dialog",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
